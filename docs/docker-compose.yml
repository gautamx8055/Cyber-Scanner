services:
  docs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    volumes:
      - ./mkdocs.yml:/app/mkdocs.yml
      - .:/app/docs
    restart: unless-stopped
